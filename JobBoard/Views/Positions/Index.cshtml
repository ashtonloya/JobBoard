@model IEnumerable<SAT.DATA.EF.Position>

@{
    ViewBag.Title = "";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "main-button" })
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobDscription)
            </th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.JobDscription)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.PositionId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.PositionId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.PositionId })
                </td>
            </tr>
        }

    </tbody>

</table>

@section scripts{
    <script>
        $(document).ready(function () {
            $('.table').DataTable();
        });</script>
}
